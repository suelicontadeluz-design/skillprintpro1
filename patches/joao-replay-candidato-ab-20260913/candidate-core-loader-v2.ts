import ts from 'npm:typescript@5.9.2';
import { createClient as preloadCreateClient } from 'https://esm.sh/@supabase/supabase-js@2';
(globalThis as any).__candidate_createClient = preloadCreateClient;
const RAW = 'https://raw.githubusercontent.com/suelicontadeluz-design/skillprintpro1/3f4505438ac63dc33b8aab5c3f5e3f4d171cfbc0/patches/joao-slot-proveniencia-escrita/candidato/index.ts';
const API = 'https://api.github.com/repos/suelicontadeluz-design/skillprintpro1/contents/patches/joao-slot-proveniencia-escrita/candidato/index.ts?ref=3f4505438ac63dc33b8aab5c3f5e3f4d171cfbc0';
const transport: typeof fetch = ((globalThis as any).__candidate_original_fetch ?? globalThis.fetch).bind(globalThis);
let s = ''; const r = await transport(RAW,{cache:'no-store'}); if(r.ok)s=await r.text();else{const api=await transport(API,{headers:{'Accept':'application/vnd.github.raw+json','User-Agent':'skillprint-replay-candidate'},cache:'no-store'});if(!api.ok)throw new Error(`candidate_core_fetch_failed:raw=${r.status},api=${api.status}`);s=await api.text();}
if(!s.includes("const SUPABASE_URL = Deno.env.get('SUPABASE_URL')!;"))throw new Error('candidate_core_shape_invalid');
function replaceOnce(label:string,before:string,after:string){const n=s.split(before).length-1;if(n!==1)throw new Error(`candidate_patch_anchor_${label}_count:${n}`);s=s.replace(before,after);}
replaceOnce('supabase_import',"import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';","const createClient = (globalThis as any).__candidate_createClient;");
replaceOnce('lead_created_var',"let leadId: string | null = null; let comprou = false; let categoriaAnuncio = '';","let leadId: string | null = null; let comprou = false; let categoriaAnuncio = ''; let leadCriadoEm: string | null = null; let leadUtmAdId = ''; let leadCampaignName = ''; let leadProductType = ''; let leadUtmContent = ''; let leadAdName = '';");
replaceOnce('lead_created_read',"    categoriaAnuncio = lead.content_category;\n    if (leadId) {","    categoriaAnuncio = lead.content_category;\n    if (leadId) {\n      try {\n        const { data: lmMeta } = await sb.from('leads_marketing').select('created_at,utm_ad_id,utm_campaign_name,product_type,utm_content,utm_ad_name').eq('lead_id', leadId).order('created_at', { ascending: false }).limit(1).maybeSingle();\n        leadCriadoEm = lmMeta?.created_at ? String(lmMeta.created_at) : null;\n        leadUtmAdId = String(lmMeta?.utm_ad_id || '').trim();\n        leadCampaignName = String(lmMeta?.utm_campaign_name || '').trim();\n        leadProductType = String(lmMeta?.product_type || '').trim();\n        leadUtmContent = String(lmMeta?.utm_content || '').trim();\n        leadAdName = String(lmMeta?.utm_ad_name || '').trim();\n      } catch {}\n    }\n    if (leadId) {");
replaceOnce('ad_created_var',"let blocoAnuncio = ''; let anuncioTexto = ''; let anuncioRecente = false;","let blocoAnuncio = ''; let anuncioTexto = ''; let anuncioRecente = false; let anuncioCriadoEm: string | null = null; let anuncioSourceId = ''; let anuncioSaudacao = ''; let anuncioRowsCount = 0; let anuncioCandidatesCount = 0;");
replaceOnce('ad_lookup',"    const { data: adRow } = await sb.from('inbound_fora_horario').select('body, created_at').in('phone', variantesTelefoneBR(phone)).not('body->externalAdReply', 'is', null).order('created_at', { ascending: false }).limit(1).maybeSingle();","    const { data: adRows } = await sb.from('inbound_fora_horario').select('body, created_at').in('phone', variantesTelefoneBR(phone)).order('created_at', { ascending: false }).limit(50);\n    anuncioRowsCount = Array.isArray(adRows) ? adRows.length : -1;\n    const ads = (Array.isArray(adRows) ? adRows : []).filter((r: any) => r?.body?.externalAdReply);\n    anuncioCandidatesCount = ads.length;\n    const adRow = ads.find((r: any) => leadUtmAdId && String(r?.body?.externalAdReply?.sourceId || '').trim() === leadUtmAdId) || ads[0] || null;");
replaceOnce('ad_created_read',"    const ad: any = (adRow as any)?.body?.externalAdReply;","    anuncioCriadoEm = (adRow as any)?.created_at ? String((adRow as any).created_at) : null;\n    const ad: any = (adRow as any)?.body?.externalAdReply;\n    anuncioSourceId = String(ad?.sourceId || '').trim();");
replaceOnce('ad_greeting_capture',"      const adSaudacao = String(ad.greetingMessageBody || '').replace(/\\s+/g, ' ').slice(0, 200);","      const adSaudacao = String(ad.greetingMessageBody || '').replace(/\\s+/g, ' ').slice(0, 200);\n      anuncioSaudacao = adSaudacao;");
replaceOnce('origin_precedence',"  const prodAnuncio = categoriaParaProduto(anuncioTexto);\n  const prodCategoria = categoriaParaProduto(categoriaAnuncio);\n  const prodOrigem = (anuncioRecente && prodAnuncio) ? prodAnuncio : (prodCategoria || prodAnuncio);\n  if (anuncioRecente && prodAnuncio && prodCategoria && prodAnuncio !== prodCategoria) categoriaAnuncio = '';","  const prodAnuncioEspecifico = produtoNaMensagem(anuncioSaudacao);\n  const prodAnuncio = prodAnuncioEspecifico || categoriaParaProduto(anuncioTexto);\n  const prodCategoria = categoriaParaProduto(categoriaAnuncio);\n  const possuiProdutoEstado = String((estado as any)?.slots?.produto || '').trim().length > 0;\n  const metaAquisicao = [[ 'product_type', leadProductType ], [ 'utm_campaign_name', leadCampaignName ], [ 'utm_content', leadUtmContent ], [ 'utm_ad_name', leadAdName ]] as Array<[string,string]>;\n  let prodMetaAquisicao: string | null = null; let prodMetaAquisicaoDetalhe: string | null = null;\n  if (!possuiProdutoEstado) for (const [detalhe, valor] of metaAquisicao) { const p = categoriaParaProduto(valor); if (p) { prodMetaAquisicao = p; prodMetaAquisicaoDetalhe = detalhe; break; } }\n  const adMs = anuncioCriadoEm ? Date.parse(anuncioCriadoEm) : NaN;\n  const leadMs = leadCriadoEm ? Date.parse(leadCriadoEm) : NaN;\n  const mesmoAd = !!(leadUtmAdId && anuncioSourceId && leadUtmAdId === anuncioSourceId);\n  const anuncioDaMesmaAquisicao = mesmoAd || (Number.isFinite(adMs) && Number.isFinite(leadMs) && Math.abs(adMs - leadMs) <= 15 * 60 * 1000);\n  const anuncioVinculado = anuncioRecente || anuncioDaMesmaAquisicao;\n  const prodOrigemOriginal = (anuncioRecente && prodAnuncio) ? prodAnuncio : (prodCategoria || prodAnuncio);\n  const prodOrigem = possuiProdutoEstado ? prodOrigemOriginal : ((anuncioVinculado && prodAnuncio) ? prodAnuncio : (prodMetaAquisicao || prodCategoria || prodAnuncio));\n  if (possuiProdutoEstado) { if (anuncioRecente && prodAnuncio && prodCategoria && prodAnuncio !== prodCategoria) categoriaAnuncio = ''; } else if (((anuncioVinculado && prodAnuncio) || prodMetaAquisicao) && prodCategoria && prodOrigem && prodOrigem !== prodCategoria) categoriaAnuncio = '';\n  if ((globalThis as any).__candidate_probe_origin === true) return { ok:true,respondeu:false,tema:'probe_origem',tools:[],candidate_origin_probe:{phone,variantes:variantesTelefoneBR(phone),lead_id:leadId,categoria_anuncio:categoriaAnuncio,possui_produto_estado:possuiProdutoEstado,lead_created_at:leadCriadoEm,lead_utm_ad_id:leadUtmAdId,lead_campaign_name:leadCampaignName,lead_product_type:leadProductType,lead_utm_content:leadUtmContent,lead_ad_name:leadAdName,prod_meta_aquisicao:prodMetaAquisicao,prod_meta_aquisicao_detalhe:prodMetaAquisicaoDetalhe,ad_rows_count:anuncioRowsCount,ad_candidates_count:anuncioCandidatesCount,ad_created_at:anuncioCriadoEm,ad_source_id:anuncioSourceId,ad_greeting:anuncioSaudacao,prod_anuncio_especifico:prodAnuncioEspecifico,prod_anuncio:prodAnuncio,prod_categoria:prodCategoria,mesmo_ad:mesmoAd,anuncio_recente:anuncioRecente,mesma_aquisicao:anuncioDaMesmaAquisicao,anuncio_vinculado:anuncioVinculado,prod_origem:prodOrigem} };");
replaceOnce('promotion_detail',"const produtoMacroOrigemResolvido = normalizarProdutoMacro(prodOrigem);\nconst produtoDeterministico = produtoMacroMensagemResolvido\n  || (!produtoMacroAnteriorResolvido ? produtoMacroOrigemResolvido : null);\nconst produtoDeterministicoFonte = produtoMacroMensagemResolvido\n  ? 'mensagem_cliente'\n  : (!produtoMacroAnteriorResolvido && produtoMacroOrigemResolvido ? 'anuncio' : null);","const produtoMacroOrigemResolvido = normalizarProdutoMacro(prodOrigem);\nconst produtoMacroAnuncioResolvido = normalizarProdutoMacro(prodAnuncio);\nconst produtoMacroMetaResolvido = normalizarProdutoMacro(prodMetaAquisicao);\nconst produtoDeterministico = produtoMacroMensagemResolvido || (!produtoMacroAnteriorResolvido ? produtoMacroOrigemResolvido : null);\nconst produtoDeterministicoFonte = produtoMacroMensagemResolvido ? 'mensagem_cliente' : (!produtoMacroAnteriorResolvido && produtoMacroOrigemResolvido ? 'anuncio' : null);\nconst produtoDeterministicoFonteDetalhe = produtoMacroMensagemResolvido ? 'mensagem_cliente' : (produtoDeterministicoFonte === 'anuncio' ? ((anuncioVinculado && produtoMacroAnuncioResolvido && produtoMacroOrigemResolvido === produtoMacroAnuncioResolvido) ? 'externalAdReply' : (produtoMacroMetaResolvido && produtoMacroOrigemResolvido === produtoMacroMetaResolvido ? prodMetaAquisicaoDetalhe : 'content_category')) : null);");
replaceOnce('persist_source_insert',"  if (estadoLog.fonte_nivel <= 2 && estadoLog.modalidade !== 'desconhecida') {","  const produtoMacroNovoPersistido = normalizarProdutoMacro(slotsNovos.produto ?? slotsAnteriores.produto);\n  const produtoFontePersistida = produtoMacroNovoPersistido && produtoMacroNovoPersistido !== produtoMacroAnteriorResolvido && slotsRecebidos.produto !== undefined ? (produtoDeterministico && produtoMacroNovoPersistido === produtoDeterministico ? produtoDeterministicoFonte : 'modelo') : null;\n  const produtoFonteDetalhePersistida = produtoFontePersistida ? (produtoFontePersistida === produtoDeterministicoFonte ? produtoDeterministicoFonteDetalhe : 'modelo_slot') : null;\n  if (produtoFontePersistida) { slotsNovos._produto_fonte = produtoFontePersistida; slotsNovos._produto_fonte_detalhe = produtoFonteDetalhePersistida; } else if (produtoMacroNovoPersistido !== produtoMacroAnteriorResolvido) { delete slotsNovos._produto_fonte; delete slotsNovos._produto_fonte_detalhe; }\n  if (estadoLog.fonte_nivel <= 2 && estadoLog.modalidade !== 'desconhecida') {");
replaceOnce('observer_source_reuse',"  const prodMacroObs = normalizarProdutoMacro(slotsNovos.produto ?? slotsAnteriores.produto);\n  // Proveniencia observavel sem schema novo: grava somente quando um produto\n  // canonico novo/mudado foi efetivamente aceito e persistido.\n  const produtoFontePersistida = prodMacroObs\n    && prodMacroObs !== produtoMacroAnteriorResolvido\n    && slotsRecebidos.produto !== undefined\n      ? (produtoDeterministico && prodMacroObs === produtoDeterministico\n          ? produtoDeterministicoFonte\n          : 'modelo_slot')\n      : null;","  const prodMacroObs = produtoMacroNovoPersistido;\n  // Proveniencia ja foi resolvida deterministicamente acima e persistida no mesmo JSONB.");
replaceOnce('short_content_guard',"function mensagemValida(m: string): boolean { const t = String(m || '').trim(); return t.length >= 2 || /^\\d$/.test(t); }","function mensagemValida(m: string): boolean { const t = String(m || '').trim(); return t.length > 0; }");
if(/^\s*import\s/m.test(s))throw new Error('candidate_unresolved_import_after_patch');
const js=ts.transpileModule(s,{compilerOptions:{target:ts.ScriptTarget.ES2022,module:ts.ModuleKind.ESNext,moduleResolution:ts.ModuleResolutionKind.Bundler,allowImportingTsExtensions:true},reportDiagnostics:true});const erros=(js.diagnostics||[]).filter((d)=>d.category===ts.DiagnosticCategory.Error);if(erros.length)throw new Error('candidate_transpile_errors:'+erros.map((d)=>d.code).join(','));await import(`data:application/javascript;charset=utf-8,${encodeURIComponent(js.outputText)}`);
