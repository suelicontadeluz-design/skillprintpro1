# Lote 2 — 14 preloads locais importados DEPOIS do núcleo (index.ts 13–26)

Fonte: edge `agente-noturno` v347, diretório `preloads/`. Relatório do subagente de análise, lido integralmente; nenhum arquivo foi modificado.


Diretório: `/tmp/claude-0/-home-user-skillprintpro1/a1334adc-5089-5cad-a537-a454c7d7b1a0/scratchpad/edge/agente-noturno/`

## Fatos gerais de ordem/encadeamento (PROVADO)
- O núcleo (`preloads__bug3__candidate-index-v338.ts`) é carregado via `index.ts:12` → `joao-v299-freight-autoquote-stack-v1.ts:12` (`./bug3/v294-v338.ts`) → `v294-v338.ts:4` → `v292.1-v338.ts:3` → `v291-v338.ts:23` (`import "./candidate-index-v338.ts"`).
- TODOS os 14 arquivos deste lote são importados em `index.ts:13–26`, ou seja, DEPOIS do núcleo. Nenhum deles toca `Deno.serve`; todos fazem só `globalThis.fetch = ...` (linhas: image-measure 121, material-capability 73, pix-gate 195, transfer-only 164, apparel 154, gold 90, material-technique 98, bag 65, sheet-choice 161, disambiguation 151, objection 67, service-mode 89, financial 167, file-state 234).
- O núcleo chama `fetch('https://api.anthropic.com/v1/messages', ...)` dinamicamente em runtime (core 3335) com `system: systemFinal` (string, core 3226) e `tools: TOOLS` (core 1247–1260), logo os wrappers instalados depois valem. Cada um guarda `baseFetch = globalThis.fetch.bind(globalThis)` no load; o ÚLTIMO importado é o mais externo. Ordem efetiva na REQUISIÇÃO: file-state-orchestrator → financial-input-provenance → service-mode-precedence → dtf-objection-continuity → product-service-disambiguation → sheet-choice → bag-material → material-technique → artwork-gold → apparel-catalog → dtf-textile-transfer-only → price-change-pix-gate → dtf-material-capability → image-measure-guard → (preloads anteriores) → Anthropic. Na RESPOSTA a ordem é inversa. Consequência: um curto-circuito sintético de um wrapper externo (ex.: bag-material) impede que os internos vejam a chamada; injeções no `system` feitas por externos são vistas pelos internos.
- Como o núcleo trata as respostas sintéticas: se `stop_reason !== 'tool_use'` ele pega o texto e faz `extrairJson` (core 3347–3355) → o JSON `{responde,mensagem,tema,...}` sintético vira a decisão. Resposta HTTP não-ok ou `type:'error'` (caso do 409 do service-mode) → `throw 'anthropic_indisponivel'` (core 3339–3342) → catch em core 3633 → `decisao` fica no default `{responde:false, tema:'ruido'}` (core 3500) → nada é enviado (INFERIDO a partir dessas linhas; em turno com imagem tenta 1x de novo sem imagem, core 3635).
- Ferramentas do núcleo (core 1248–1259): consultar_catalogo, calcular_copo, consultar_tabela_dtf, calcular_dtf_por_arte, calcular_dtf_metro, calcular_dtf_uv_metro, calcular_rendimento_uv, consultar_modelos, orcar_camisetas, calcular_frete, gerar_pix, compor_total.
- Padrão comum a quase todos: só agem se `typeof req.system==='string' && Array.isArray(req.messages)`; "inbound" = última msg `user` sem `tool_result` e que não começa com `[SISTEMA:`.
- `[DTF_FILE_PREFLIGHT_CANONICO]` (lido por image-measure 63 e financial 101): produtor NÃO ENCONTRADO em nenhum arquivo local (só os dois leitores); provavelmente vem de import remoto `pricing-current-turn-preflight-v1.ts` (freight stack :5) — INFERIDO.
- Segredos: env `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `ERP_URL`, `ERP_SERVICE_KEY`/`ERP_SERVICE_ROLE_KEY`. Não copiados. Nota: `ERP_URL` tem default hardcoded apontando para um projeto Supabase distinto do `SUPABASE_URL` (material-capability 8, transfer-only 9, apparel 10, sheet-choice 8).

---

### 1. preloads__joao-image-measure-provenance-guard-v1.ts
- **Mecanismo de hook**: `globalThis.fetch` (121) só para `^https://api.anthropic.com/v1/messages` (123). Pós-resposta (deixa a requisição intacta, 134). Depois do núcleo (index.ts:13).
- **Injeção no prompt**: NENHUMA (não altera `system`/`messages`).
- **Condição de inclusão**: só quando a mensagem atual do cliente tem imagem (`content.type==='image'` ou regex `(foto enviada pelo cliente)|[imagem]|imagem enviada|foto enviada`, 43, 131) E a medida NÃO foi provada por: texto do cliente nas últimas 24 msgs com `AxB cm/mm` ou `AxB` perto de medida/tamanho/arte/estampa/logo (50–60), OU bloco `[DTF_FILE_PREFLIGHT_CANONICO]` no system com `NxNcm` e sem `dimensao_nao_provada` (61–69). Sem flag em `sistema_config`. Fail-open (qualquer parse falho → passa).
- **Tools**: intercepta `tool_use` de `calcular_dtf_por_arte|calcular_dtf|calcular_rendimento_uv` (76) com largura/altura >0 (79–84) → substitui a resposta inteira (140–145). Não altera lista de tools.
- **Leituras/escritas de banco**: nenhuma.
- **Efeito na resposta final**: substitui por JSON determinístico `responde:true, mensagem:'Consigo analisar a arte, mas a imagem não prova a medida física. Qual é a medida da arte em cm, altura x largura?', tema:'complexo', etapa:'sondagem', stop_reason:'end_turn'` (86–100). Também reescreve texto: se `mensagem` do modelo contém padrão `AxB` (70–71) troca pela mesma pergunta (102–118). Header `x-cortex-image-measure-guard`.
- **Decisões comerciais**: (medidas) imagem não prova dimensão física → pergunta medida; (DTF) bloqueia cálculo têxtil/UV sem medida provada; (etapa) força `sondagem` e `encaminhou_venda:false`.
- **"Skill" real?**: n/a (guard).
- **Duplicidade/conflito**: core 2207 "VOCÊ ENXERGA IMAGENS: PROIBIDO dizer que não consegue ver" vs. a mensagem sintética ("a imagem não prova a medida") — não contradiz, mas o guard reescreve QUALQUER `mensagem` com `NxN` em turno com imagem (71 não exige cm), inclusive opções proporcionais que o core manda oferecer (core 2211 "ofereça opções proporcionais", core 2201 "OPÇÃO QUE VOCÊ OFERECEU (7x3,1cm...)") → conflito PROVADO (preload 71/109 x core 2201/2211). Sobreposição com financial-input-provenance (bloqueia `calcular_rendimento_uv` sem dimensão provada, financial 189–194) — dois guards para o mesmo tool com regex de prova diferentes (image 53–56 x financial 82–95).

### 2. preloads__joao-dtf-material-capability-skill-v1.ts
- **Mecanismo de hook**: `globalThis.fetch` (73) para Anthropic `/v1/messages` (75). Pré-requisição (muta `system`). Depois do núcleo (index.ts:14).
- **Injeção no prompt**: acrescenta ao fim do `system` o bloco `[SKILL DTF MATERIAL — FONTE ERP] ... [/SKILL DTF MATERIAL]` (60): linhas dinâmicas por técnica (`tecnica=|compativeis=|incompativeis=|aplicacao=|ferro_domestico=|prensa_recomendada=|prensa_obrigatoria=|precisa_esclarecer=`) + texto fixo ≈ 300 chars ("Use SOMENTE estes dados para compatibilidade/aplicação... Preço NÃO pertence a esta skill...").
- **Condição de inclusão**: inbound bate regex de material/aplicação `dtf|tecido|algodão|poliéster|papel|plástico|vidro|metal|madeira|mdf|acrílico|copo|caneca|garrafa|sacola|ecobag|ferro|prensa|aplica...|cola|substrato|material` (40, 82) E ERP respondeu com `perfil.capability_v1`. Sem `sistema_config`. Sem ERP_KEY ou ERP fora → não injeta, segue normal (fail-open, 63, 85–88).
- **Tools**: nenhuma alteração.
- **Leituras/escritas**: LEITURA ERP `GET /rest/v1/tecnicas?select=slug,nome,perfil&slug=in.(dtf-textil,dtf-uv)&ativo=eq.true` (65). Sem escrita.
- **Efeito na resposta final**: nenhum (não toca a resposta).
- **Decisões comerciais**: (DTF/diagnóstico) fonte de compatibilidade material→técnica é o ERP; (pergunta) manda perguntar material se estiver em `precisa_esclarecer`; (preço) declara que preço não é da skill.
- **"Skill" real?**: NÃO. Sem manifesto/descoberta; é bloco de texto injetado por regex + dados do ERP. A única "progressive disclosure" é a ativação por regex (40).
- **Duplicidade/conflito**: core 2232–2233 (FICHA: "DTF UV: adesivo pronto para copo, vidro, metal, madeira, MDF, acrílico. Sem prensa" / "DTF TÊXTIL: película para tecido, precisa de prensa") e core 2264 ("NUNCA MISTURE AS TABELAS: adesivo, copo... = UV. Camiseta, tecido... = TEXTIL") coexistem com o bloco ERP que manda "Use SOMENTE estes dados" (60) → se o ERP divergir do texto fixo, conflito de fonte PROVADO. Regex de ativação (40) se sobrepõe à do material-technique-gate (46–47) e bag-gate (42–44).

### 3. preloads__joao-price-change-pix-gate-v1.ts
- **Mecanismo de hook**: `globalThis.fetch` (195) para Anthropic (197). Pós-resposta. Depois do núcleo (index.ts:15). Só exige `messages` array (202).
- **Injeção no prompt**: NENHUMA.
- **Condição de inclusão**: resposta contém `tool_use gerar_pix` (181–183, 208) com `operation_id` UUID (211–212). Passa direto (não bloqueia) se: a msg anterior do assistente era a explicação do próprio gate ("O valor atualizado ficou em R$"+"Posso gerar o Pix nesse valor?", 73–75) E inbound é afirmação (`sim|ok|pode|manda o pix|...`, 69–71) → 216–220. Bloqueia se `diff > R$10 && pct > 20%` (230) OU mudança "material" (122–155: formato UV, modelo camiseta, textil↔uv, unidade de venda, peça pronta↔filme). Sem `sistema_config`. Sem URL/service key ou falha DB → retorna null → não bloqueia (fail-open, 101, 223, 225).
- **Tools**: intercepta `gerar_pix` (substitui a resposta inteira, 243–245). Não altera lista.
- **Leituras/escritas**: LEITURA `operacoes_financeiras` por id (103) e por lead_id/kind in (produto,total,orcamento_calcme_entrada)/created_at<cur (112–116) — usa `SUPABASE_URL` + service role. ESCRITA `POST /rest/v1/sistema_logs` (156–168, funcao 'price-change-pix-gate', categoria 'financial_guard', effect_class 'PAYMENT_BLOCK') em ambos os caminhos (218, 234).
- **Efeito na resposta final**: substitui por JSON `tema:'fechamento_pix', etapa:'fechamento', mensagem:'O valor atualizado ficou em R$ X; antes estava em R$ Y, diferença de R$ Z, porque <motivo>. Posso gerar o Pix nesse valor?'` (185–192). Motivos gerados em 79–98/122–155. Header `x-cortex-price-change-pix-gate`. Só compara com valor "anterior" que apareceu literalmente em `R$` em texto do assistente nas últimas 8 msgs (41–57, 170–179).
- **Decisões comerciais**: (Pix/pagamento) bloqueia gerar_pix quando preço subiu >20% e >R$10 vs último mostrado; (Pix) bloqueia quando produto/formato/modelo mudou; (fechamento) exige confirmação explícita do cliente antes do Pix; (preço) verbaliza valor novo/antigo/diferença pegos do banco (o próprio arquivo diz "No price authority" mas ESCREVE valores em R$ na mensagem, 190).
- **"Skill" real?**: n/a.
- **Duplicidade/conflito**: core 2224 "4. gerar_pix... Você NUNCA informa valor ao gerar_pix" e core 2197 "PROIBIDO escrever 'vou gerar o Pix' e terminar sem o código" / core 2274 "PROMESSA DE PIX: NUNCA escreva que vai gerar o Pix sem gerar" — o gate troca um `gerar_pix` executável por uma pergunta ("Posso gerar o Pix nesse valor?") → conflito PROVADO (preload 190 x core 2197/2274). Core 2225 "PIX: ... Confirma AUTOMATICAMENTE" vs exigência de confirmação (216) — conflito PROVADO.

### 4. preloads__joao-dtf-textile-transfer-only-skill-v1.ts
- **Mecanismo de hook**: `globalThis.fetch` (164) para Anthropic (166). Pré (remove tools, injeta system) + pós (reescreve). Pode curto-circuitar. Depois do núcleo (index.ts:16).
- **Injeção no prompt**: bloco `[SKILL DTF TEXTIL TRANSFER ONLY — FONTE ERP] ... [/SKILL DTF TEXTIL TRANSFER ONLY]` (107–126): campos ERP `service_mode=|delivery_form=|individually_cut=|customer_cuts_transfers=|required_inputs=|irrelevant_garment_inputs=` + 5 regras fixas ≈ 420 chars ("Não pergunte cor, modelo, tamanho, grade ou marca...", "Para orçamento, use medida da arte + cópias OU metragem direta.", "Não orce peça pronta neste turno.", "O filme é entregue impresso na metragem contratada e não cortado individualmente; o recorte é do cliente.", "Preço não pertence a esta skill"). Só injeta se ERP devolveu `metadata.transfer_only_capability_v1` (109, 189).
- **Condição de inclusão**: `dttoTransferOnly` (52–74): NÃO se inbound diz `dtf uv|adesivo uv`; exige têxtil estabelecido (inbound com `dtf textil|filme dtf|transfer` ou `dtf`+tecido/camiseta..., OU histórico de 14 msgs com `dtf textil`) E filme explícito (`só/somente/apenas ... filme|impressao|transfer|dtf`, `eu mesmo aplico`, `por metro|metragem|metros`, `quero/preciso ... filme|transfer dtf`, `para eu aplicar ... camisas`) E NÃO peça pronta explícita (70–71). Sem `sistema_config`. Remoção de tools acontece MESMO se ERP falhar (184–186); bloco de system só com ERP (fail-open no texto, fail-closed nas tools).
- **Tools**: REMOVE da lista `orcar_camisetas, consultar_modelos, calcular_copo, calcular_rendimento_uv, calcular_dtf_uv_metro` (12–15, 184–186). Se mesmo assim o modelo emitir `tool_use` de um deles, troca o bloco por texto JSON (135–139) e força `end_turn` (161).
- **Leituras/escritas**: LEITURA ERP `GET /rest/v1/produtos?select=sku,nome,metadata&sku=eq.DTF-TXT&ativo=eq.true` (101). Sem escrita.
- **Efeito na resposta final**: (a) curto-circuito determinístico se inbound pergunta corte (`cortad|recortad|recorte|pronto para aplicar` + `filme|dtf|transfer|estampa|arte`, 75–79) e ERP diz `individually_cut===false`: `'Não. O filme DTF têxtil é entregue impresso na metragem contratada, sem corte individual[. O recorte é feito por você.]'` tema `dtf_metro` (80–95, model 'cortex-dtf-transfer-only'); (b) tool_use bloqueado → `'Para o filme DTF têxtil eu não preciso de cor, modelo ou tamanho da camiseta. Me passa a medida da arte e quantas cópias você precisa, ou a metragem direta.'` (137); (c) mensagem que diz que "vai/vem/entregue ... cortado/pronto para aplicar" → apaga a frase e anexa "O filme é entregue impresso na metragem contratada, sem corte individual; o recorte é feito por você." (144–151); (d) mensagem que pergunta `cor|modelo|tamanho|grade|marca` sem citar arte/estampa/logo → substitui pela decisão (b) (145–155). Header `x-cortex-dtf-textile-transfer-only`.
- **Decisões comerciais**: (modo de serviço) intenção transfer-only atual vence contexto de camiseta; (camiseta) proíbe orçar/perguntar peça; (DTF) define que filme não é cortado; (medidas/quantidade) pede medida+cópias ou metragem; (multi-item/UV) remove ferramentas UV mesmo se cliente misturar; (etapa) força sondagem.
- **"Skill" real?**: NÃO. Regex + bloco fixo + remoção de tools + reescrita; sem manifesto/descoberta.
- **Duplicidade/conflito**: core 2234 "CAMISETA...: Colete modelo, cor, grade..." vs preload 119 "Não pergunte cor, modelo, tamanho" (condicional, mas conflito quando o regex 59–68 dispara falsamente, ex. "por metro" sozinho com histórico `dtf textil`). Core 2267 "CLIENTE COM ARQUIVO MONTADO: ... NAO pergunte tamanho de estampa nem quantas copias" vs preload 120/137 "Me passa a medida da arte e quantas cópias" → conflito PROVADO quando cliente já tem arquivo montado (também colide com file-state-orchestrator 170–174 que proíbe exatamente essa pergunta). Remoção de tools UV (12–15) duplica/compete com material-technique-gate (108–114) e service-mode (core.mjs 157–165: remove `orcar_camisetas, consultar_modelos` em TRANSFER) — três remoções de tools por regexes diferentes.

### 5. preloads__joao-apparel-catalog-skill-v1.ts
- **Mecanismo de hook**: `globalThis.fetch` (154) para Anthropic (156). Pré (system + descrição de tool) e pós (reescrita). Depois do núcleo (index.ts:17).
- **Injeção no prompt**: bloco `[SKILL APPAREL CATALOG — FONTE ERP] ... [/SKILL APPAREL CATALOG]` (94–119): `produto=|faixas_quantidade=|descricao_curta=|aviso_venda=|composicao=|corpo_cor_unica=SIM|detalhe_contrastante_gola_manga=NAO|NAO_AFIRMAR_SEM_FONTE=` + 4 regras fixas ≈ 330 chars ("Quantidade mínima/faixa vem destas faixas do ERP, nunca de regra memorizada.", "Mesmo para 1 a 9 peças, não recuse por memória: chame orcar_camisetas...", "Preço não pertence a esta skill", "Se um atributo não estiver neste bloco, não invente").
- **Condição de inclusão**: inbound cita vestuário (`camiseta|camisa|baby look|polo|oversized|moletom|canguru|plus|infantil|algodao|malha`) E (pergunta de `minimo|quantidade|quantas|material|tecido|composicao|...|gola|manga|costura|acabamento` OU quantidade explícita `N camisetas/peças/...` ≤10000) (44–56). E produto resolvido no ERP (164). Sem `sistema_config`. Sem ERP → não faz nada (fail-open).
- **Tools**: REESCREVE `description` de `orcar_camisetas` (120–130): tenta remover "O minimo de 10 pecas vale por GRUPO DE ESTAMPA, somando modelos diferentes." (regex 124 — PROVADO que o texto existe na descrição do core 1256, logo a remoção é efetiva) e anexa "A validade da quantidade e determinada pelo ERP... NAO aplique minimo memorizado. Chame esta ferramenta mesmo para 1 a 9 pecas... Faixas do produto atual: <ranges>". Não bloqueia execução.
- **Leituras/escritas**: LEITURA ERP `GET /rest/v1/produto_apelidos?select=apelido,produto_id&limit=500` (64), `GET /rest/v1/produtos?select=id,nome,metadata,aviso_venda,descricao_curta&ativo=eq.true&id=eq.<id>` (77), RPC `POST /rest/v1/rpc/fn_faixas_validas_texto {p_produto_id}` (82–86). Sem escrita.
- **Efeito na resposta final**: se ranges contém "a partir de 1 peça" e a `mensagem` afirma mínimo 10 / recusa 6–9 / "a partir de 10" (131–134) → substitui mensagem por `'Essa quantidade (N) está dentro da faixa do ERP para <produto>. Vou seguir pelo orçamento oficial e pedir apenas o que ainda faltar da estampa.'` ou `'A quantidade deve seguir a faixa do ERP deste produto. Não vou aplicar mínimo antigo por memória.'` (143–145), `encaminhou_venda:false`. Header `x-cortex-apparel-catalog`.
- **Decisões comerciais**: (quantidade/mínimo) mínimo vem do ERP, não do prompt; (camiseta) ordena chamar orcar_camisetas mesmo com 1–9 peças; (diagnóstico) atributos de produto só do ERP; (correferência) resolve produto por apelido mais longo ou "camiseta" genérica (67–75).
- **"Skill" real?**: NÃO. Bloco fixo + dados ERP por regex; sem manifesto.
- **Duplicidade/conflito**: core 1256 (descrição orcar_camisetas "O minimo de 10 pecas vale por GRUPO DE ESTAMPA") e core 2265 (REGRAS_EXTRA "O minimo e validado por grupo de estampa, somando modelos diferentes") — o preload apaga a primeira (124) mas NÃO a segunda em REGRAS_EXTRA → contradição residual PROVADA (preload 112–113 x core 2265). Core 2268–2270 "VALIDACAO DE CATALOGO: chame consultar_modelos ANTES" vs transfer-only (remove `consultar_modelos`) e service-mode TRANSFER (remove `consultar_modelos`) — quando apparel-catalog e um deles disparam no mesmo turno, o system pede tool que foi removida.

### 6. preloads__joao-artwork-gold-skill-v1.ts
- **Mecanismo de hook**: `globalThis.fetch` (90) para Anthropic (92). Pré (system) + pós (reescrita) ou curto-circuito. Depois do núcleo (index.ts:18).
- **Injeção no prompt**: bloco `[SKILL ARTWORK GOLD v1] ... [/SKILL ARTWORK GOLD]` (63–67), versão "não-físico" ≈ 230 chars ("Dourado pode ser representado como TOM DOURADO DIGITAL na arte. NÃO chame isso de tinta metálica, foil ou efeito metálico real..."). A versão "físico" (65) NUNCA é injetada: quando `physical` é true a função retorna antes (100–103) — código morto PROVADO.
- **Condição de inclusão**: inbound com `dourad[oa]|gold|ouro|metálic[oa]|metalizad[oa]|brilho dourado|efeito ouro` (38, 97). Sem `sistema_config`. Sem DB.
- **Tools**: nenhuma alteração.
- **Leituras/escritas**: nenhuma.
- **Efeito na resposta final**: (a) se pedido físico explícito (`tinta metálica|metalizado real|efeito metálico|foil|hot stamp|dourado espelhado|...`, 40–45) → curto-circuito sem chamar Anthropic: `'Você precisa de efeito metálico real ou um dourado visual na arte já atende?'` tema `complexo`, etapa `sondagem` (46–62, model 'cortex-artwork-gold'); (b) se a mensagem do modelo afirma `tinta metálica|metálico real|metalizado real|foil|brilho metálico` (76) → substitui por `'Consigo fazer o dourado como tom visual na arte digital. Não é tinta metálica nem efeito metálico real.'` (81), `encaminhou_venda:false`. Header `x-cortex-artwork-gold`.
- **Decisões comerciais**: (arte) dourado = tom digital; (diagnóstico) nunca prometer metálico/foil; (pergunta) uma pergunta de esclarecimento quando efeito físico.
- **"Skill" real?**: NÃO. Regex + bloco fixo.
- **Duplicidade/conflito**: core 2237 "NÃO FAZEMOS: silk, serigrafia, sublimação, bordado, boné" não menciona foil/metálico — o preload preenche lacuna, sem par direto. Core 2214 "UMA pergunta por mensagem" compatível. Curto-circuito (102) ignora o contexto (ex.: cliente com Pix pendente que fala "dourado metálico") — sem checagem de etapa (INFERIDO como risco).

### 7. preloads__joao-dtf-material-technique-gate-v1.ts
- **Mecanismo de hook**: `globalThis.fetch` (98) para Anthropic (100). Pré (remove tools, system) + pós. Depois do núcleo (index.ts:19).
- **Injeção no prompt**: `[CORTEX MATERIAL TECHNIQUE GATE v1] ... [/CORTEX MATERIAL TECHNIQUE GATE]` (52–55), ≈ 200 chars: têxtil → "O material explicitamente informado é de contexto têxtil. A técnica compatível é DTF Têxtil. DTF UV é incompatível neste caso. Use apenas ferramentas têxteis disponíveis."; rígido → "...é rígido/compatível com DTF UV. Use DTF UV. Não use ferramentas de DTF Têxtil...".
- **Condição de inclusão**: inbound (só a última msg) com termos têxteis (`tecido|algodao|poliester|camiseta|camisa|roupa|malha|pano`) sem rígidos, ou rígidos (`vidro|metal|madeira|mdf|acrilico|copo|caneca|garrafa`) sem têxteis (44–50). Ambos → não age. Sem `sistema_config`, sem DB.
- **Tools**: REMOVE `calcular_rendimento_uv, calcular_dtf_uv_metro` (têxtil) ou `calcular_dtf_por_arte, calcular_dtf_metro` (UV) (10–11, 108–114). Se o modelo ainda emitir tool_use "errado", troca por texto (67–81) e força `end_turn` (95).
- **Leituras/escritas**: nenhuma.
- **Efeito na resposta final**: tool errado → `'Para esse material, o correto é DTF têxtil. Me passa a medida da arte e a quantidade para eu calcular pelo sistema.'` / `'...DTF UV. Me passa a medida e a quantidade...'` tema `sondagem` (74–77). Mensagem que cita a técnica oposta (`dtf uv` em contexto têxtil, ou `dtf textil` em contexto UV, 57–61) → substitui por `'Para esse material, o correto é DTF têxtil.'`/`'...DTF UV.'` (85–87). Header `x-cortex-material-technique-gate`.
- **Decisões comerciais**: (DTF/diagnóstico) mapeia material→técnica de forma fixa; (camiseta) "camiseta" na frase = têxtil → remove UV; (medidas/quantidade) pede medida+quantidade; (multi-item) frase com ambos os grupos → não age.
- **"Skill" real?**: n/a (gate). Contradiz o comentário 4–6 ("ERP capability skill remains the source of truth") — o mapeamento aqui é hardcoded (46–47), não ERP.
- **Duplicidade/conflito**: DUPLICA core 2264 "NUNCA MISTURE AS TABELAS: adesivo, copo, caneca, vidro, metal, madeira, MDF, acrilico = UV. Camiseta, tecido, malha, pelicula = TEXTIL" e core 2232–2233 (preload 46–47 x core 2264). Conflito com dtf-material-capability (ERP como fonte, 60) — se ERP disser que algodão é incompatível com dtf-textil, este gate ainda força têxtil. Reescrita 82–91 apaga a mensagem inteira (inclusive preço/pergunta) só porque citou "DTF UV" numa explicação comparativa → conflito com core 2257 "MUDANCA DE ASSUNTO: RESPONDA SOBRE ELE com preco" (PROVADO por regex 59–60). Camiseta do cliente com copo na mesma frase → null (50), então o gate silencia em multi-item.

### 8. preloads__joao-bag-material-clarification-gate-v1.ts
- **Mecanismo de hook**: `globalThis.fetch` (65) para Anthropic (67). Curto-circuito puro (nunca chama Anthropic quando dispara). Depois do núcleo (index.ts:20). Só exige `messages` (70).
- **Injeção no prompt**: NENHUMA.
- **Condição de inclusão**: inbound com `sacola|ecobag|bolsa(s)` (42) e SEM material (`tecido|algodao|poliester|papel|plastico|vidro|metal|madeira|mdf|acrilico|lona|tnt|nylon|couro`, 44). Sem `sistema_config`, sem DB. Sem verificação de histórico (se o cliente já disse o material em msg anterior, dispara de novo — PROVADO pela leitura só do inbound 27–36).
- **Tools**: nenhuma.
- **Leituras/escritas**: nenhuma.
- **Efeito na resposta final**: substitui por `'Qual é o material da sacola? Preciso disso para indicar o DTF correto.'` tema/etapa `sondagem` (47–62, model 'cortex-bag-material-gate'). Header `x-cortex-bag-material-gate`.
- **Decisões comerciais**: (diagnóstico/DTF) sacola sem material → pergunta antes de qualquer coisa; (pergunta de preço) ignora pedido de preço se tiver "sacola" (ex.: "quanto custa 100 sacolas?" vira pergunta) — PROVADO por 72–76.
- **"Skill" real?**: n/a.
- **Duplicidade/conflito**: core 2213 "ANTI-ENROLAÇÃO: pediu valor ou tabela, a PRÓXIMA MENSAGEM TEM NÚMERO EM R$" e core 3223 `[O CLIENTE JÁ PEDIU PREÇO...]` vs curto-circuito sem preço (76) → conflito PROVADO. Core 2271 "NAO REPERGUNTE O QUE JA FOI RESPONDIDO" vs disparo repetido em cada turno com "sacola" sem material na mesma msg (40–46). Sobrepõe dtf-material-capability (regex 40 inclui sacola/ecobag) — mas como o bag-gate é mais externo (index 20 > 14), ele corta antes e a skill ERP nunca roda nesse turno.

### 9. preloads__joao-sheet-choice-continuity-gate-v1.ts
- **Mecanismo de hook**: `globalThis.fetch` (161) para Anthropic (163). Pré (system) + pós (reescrita). Depois do núcleo (index.ts:21).
- **Injeção no prompt**: `[CORTEX SHORT CHOICE CONTINUITY v1] ... [/CORTEX SHORT CHOICE CONTINUITY]` (93–104) ≈ 300–420 chars: "A mensagem atual do cliente é "A3/A4" e responde diretamente à pergunta anterior... Considere A3 CONFIRMADO... NÃO pergunte A3/A4 novamente..." + (se contexto folha) "O contexto é folha DTF UV. Preço unitário canônico ERP da folha A3: R$ X. A escolha do formato sozinha NÃO prova quantas folhas..." + "Siga apenas para o próximo dado realmente faltante."
- **Condição de inclusão**: inbound é exatamente `a3`/`a4` (41–46) E a msg anterior do assistente cita A3 e A4 com `?` ou `qual|prefere|escolhe|tamanho|folha|formato` (56–60). `sheet` = anterior cita `dtf uv|folha(s)|adesivo(s)|por metro|metragem` (61–63). Sem `sistema_config`. ERP indisponível → sem preço (fail-open).
- **Tools**: nenhuma.
- **Leituras/escritas**: LEITURA ERP RPC `POST /rest/v1/rpc/fn_cortex_pricing_explicit_sheet_quote_v1 {p_format, p_count:1}` (69–73), aceita só `ok && canonical && system_of_record==='ERP'` (76). ESCRITA `POST /rest/v1/sistema_logs` (79–92, funcao 'sheet-choice-continuity', categoria 'qualification_runtime', evento 'short_sheet_choice_corrected') só quando reescreve (186).
- **Efeito na resposta final**: reescreve `mensagem`: remove frases que reperguntam A3/A4 (`REASK_REMOVED`, 141–145, fallback "Perfeito, ficou A3."); corrige formato errado ("perfeito... A4" quando escolheu A3 → troca, `WRONG_FORMAT_CORRECTED`, 146–150); substitui valor em R$ de frase de preço unitário da folha pelo `unit_price` do ERP (`ERP_UNIT_PRICE_CORRECTED`, 115–132, 151–154). Força `responde:true`, `encaminhou_venda:false`, `end_turn` (155, 158). Header `x-cortex-sheet-choice-continuity`.
- **Decisões comerciais**: (correferência) "A3"/"A4" curto = resposta à escolha anterior; (preço) injeta e corrige preço unitário de folha pelo ERP (sobrescreve o número do modelo — autoridade de preço PROVADA em 129); (quantidade) formato não prova contagem de folhas; (pergunta) proíbe reperguntar.
- **"Skill" real?**: n/a.
- **Duplicidade/conflito**: core 2259 "UMA PALAVRA: mensagem de uma palavra ou numero e SEMPRE resposta do cliente" e core 3156 (regra em código) — DUPLICADO (preload 41–46 x core 2259). Core 2232 dá preços fixos de folha ("A4 R$29,90; A3 R$39,00") e core 2255 ("folha A3 de R$39,00") vs ERP `unit_price` injetado (100) — se divergirem, o preload sobrescreve o texto do modelo (129) → conflito de fonte PROVADO. Core 2189 "SEMPRE use calcular_rendimento_uv... verbalize somente o total devolvido" vs correção de preço unitário por regex (115–132) sem tool.

### 10. preloads__product-service-disambiguation-preload-v3-user-evidence.ts + preloads__product-service-disambiguation-context-core-v1.ts
- **Mecanismo de hook**: `globalThis.fetch` (151) para Anthropic (153). Curto-circuito puro. Depois do núcleo (index.ts:22). Core `-context-core-v1.ts` é puro (sem I/O), exporta `pdContextEvidence` (54–67).
- **Injeção no prompt**: NENHUMA.
- **Condição de inclusão**: FLAG `sistema_config.chave='joao_qualification_gate_ativo'` (`valor_bool===true`), cache 15s (29–41); ausente/erro → `false` → FAIL-CLOSED (desliga o preload). Depois: (a) cliente "já tem" copo/caneca/garrafa ou camisa/camiseta/roupa/peça E cita `figurinha|adesivo|dtf|transfer` (96–105) → resposta direta; (b) senão, se modo explícito no inbound (`dtf|transfer|adesivo|folha|metro`, `só impressão`, "aplicar em tecido", peça pronta/personalizada, 76–95) → passa; (c) senão, `shouldClarify` = nenhum modo explícito no inbound nem nas 10 msgs anteriores do cliente E histórico do cliente cita dtf/adesivo copo/impressão tecido (core 54–66) E inbound cita apparel ou drinkware (113–119) → clarificação.
- **Tools**: nenhuma.
- **Leituras/escritas**: LEITURA `GET /rest/v1/sistema_config?select=valor_bool&chave=eq.joao_qualification_gate_ativo` (33). ESCRITA `POST /rest/v1/sistema_logs` (127–141, funcao 'product-service-disambiguation', evento 'product_service_ambiguity_clarified', categoria 'skill_runtime') nos dois caminhos (164, 184).
- **Efeito na resposta final**: (a) `'Perfeito, então é só o adesivo DTF UV para aplicar no copo/caneca que você já tem. Qual é a medida do adesivo em cm, altura x largura?'` (tema `adesivo_uv`, slots.produto `dtf_uv`) ou `'Perfeito, então é só o DTF têxtil para aplicar na camiseta/peça que você já tem. Qual é a medida da arte em cm, altura x largura?'` (tema `dtf_metro`, produto `dtf_textil`) (106–112); (b) clarificação: `'Perfeito — só pra eu não te passar o preço errado: você quer somente as impressões para aplicar (DTF UV para caneca e DTF têxtil para camisa) ou quer as canecas e camisetas já personalizadas?'` / `'Só pra eu não te passar o preço errado: você quer o adesivo DTF UV para aplicar na caneca/copo ou quer a peça já personalizada?'` / `'...a impressão DTF têxtil para aplicar na camisa/camiseta ou quer a peça já personalizada?'` (120–126). Model 'cortex-qualification-disambiguation'.
- **Decisões comerciais**: (modo de serviço) transfer-only vs peça pronta; (classificação de intenção) "já tenho a peça" + adesivo/dtf = transfer; (medidas) pede medida em cm altura x largura; (correferência) usa só msgs do cliente, ignora o que o assistente disse; (pergunta de preço) clarificação prevalece sobre pedido de preço.
- **"Skill" real?**: n/a (nome "preload"); o log chama de `skill_ref:'qualification'` (136) mas é regex + flag.
- **Duplicidade/conflito**: DUPLICA core 2195 "CLIENTE QUE JÁ TEM A PEÇA: quando ele fala do copo, caneca... DELE, o produto é o ADESIVO DTF UV" (preload 96–111 x core 2195). Core 2213/2259 (preço na próxima msg) vs clarificação (176–185). Sobrepõe service-mode-precedence (core.mjs 62–101 detecta os mesmos sinais e injeta system) e dtf-objection (regex de transfer-only) — três detectores de modo com regexes distintos: disambiguation 76–95, service-mode.mjs 67–87, transfer-only 52–74.

### 11. preloads__joao-dtf-objection-continuity-skill-v1.ts + preloads__joao-dtf-objection-continuity-core-v1.mjs
- **Mecanismo de hook**: `globalThis.fetch` (67) para Anthropic (69). Curto-circuito puro. Depois do núcleo (index.ts:23). `.mjs` é puro (regex).
- **Injeção no prompt**: NENHUMA.
- **Condição de inclusão**: ≥2 msgs do cliente (75). `jocMatch` (mjs 42–46): histórico do cliente cita `dtf textil` E metro/metragem/N metros (mjs 13–19) E inbound é objeção (`outra empresa|outro fornecedor|nao transfere|nao cola|nao adere|estragou|perdi dinheiro|perdi a camiseta|medo|receio|problema`, mjs 21–25) E não é troca explícita para peça pronta (mjs 27–30). OU (skill 51–56, 80): assistente já disse `dtf textil` E cliente citou `N metros` antes E objeção E não troca. Sem `sistema_config`, sem DB.
- **Tools**: nenhuma.
- **Leituras/escritas**: nenhuma.
- **Efeito na resposta final**: substitui por texto FIXO: `'Desculpa, me expressei mal na mensagem anterior. Você já confirmou 1 metro de DTF têxtil. O nosso DTF é linha premium e eu te ajudo na aplicação; em qual produto ou tecido você tentou aplicar o material da outra empresa?'` tema `dtf_metro`, etapa `sondagem`, slots `{produto:'dtf_textil', quantidade:<último "N metros">}` (84–91). Model 'cortex-dtf-objection-continuity'. Header `x-cortex-dtf-objection-continuity`.
- **Decisões comerciais**: (objeção) reclamação de fornecedor anterior = objeção, não troca de produto; (quantidade) grava `quantidade` do último "N metros" no slot; (DTF) afirma "linha premium" e "eu te ajudo na aplicação"; (texto) a mensagem diz sempre "1 metro" mesmo que `quantity` seja 2 metros (86 hardcoded vs 83) → BUG PROVADO; a mensagem pede desculpas "me expressei mal" sem verificar se o assistente disse algo errado (INFERIDO). Palavra "problema"/"medo" sozinha dispara (mjs 24).
- **"Skill" real?**: NÃO. Nome "skill" mas é regex + resposta fixa; sem manifesto.
- **Duplicidade/conflito**: core 2235 "ESTAMPARIA (cliente traz a própria peça e nós aplicamos): você NÃO cota. Quem passa o valor é a Tamires" vs "eu te ajudo na aplicação" (86) — ambíguo/INFERIDO. Core 2187 "SIGA O CLIENTE, SEMPRE... Se perguntar de outro produto, responda sobre esse produto" vs a skill que impede a troca para peça pronta a menos que bata regex estrita (mjs 29) → conflito PROVADO. Sobrepõe service-mode (mesmo tema, mode TRANSFER) e blocoObjecao do core (core 3170, `[O CLIENTE ACHOU CARO...]`) — o núcleo trata objeção de preço; este trata objeção de qualidade.

### 12. preloads__joao-service-mode-precedence-preload-v1.ts + preloads__joao-service-mode-precedence-core-v1.mjs
- **Mecanismo de hook**: `globalThis.fetch` (89) para Anthropic (91); ignora `stream:true` (98). Pré (remove tools + system) + pós (reescrita/409). Depois do núcleo (index.ts:24). `.mjs` puro.
- **Injeção no prompt**: `[CANONICAL SERVICE MODE — P0] ... [/CANONICAL SERVICE MODE — P0]` (53–61): FINISHED ≈ 330 chars ("mode=FINISHED_PERSONALIZED / produto_macro=camiseta / DTF textil citado depois desta escolha e tecnica de estampagem, NAO troca o pedido para DTF avulso por si so... Preco de camiseta vem do fluxo/orcamento de camiseta; nao use tabela/rendimento de DTF avulso."); TRANSFER ≈ 180 chars ("mode=TRANSFER_ONLY / produto_macro=dtf_textil / O cliente escolheu somente a impressao/transfer. Nao transforme em camiseta pronta sem nova manifestacao explicita.").
- **Condição de inclusão**: `smResolve` (mjs 109–155): percorre TODAS as msgs do cliente; último sinal forte vence (owns piece + camisa / self-apply / "só dtf" → TRANSFER; camiseta pronta/personalizada, "quanto custa cada camiseta", "quero camiseta" sem aplicar → FINISHED; ou resposta à oferta de modos do assistente, mjs 62–101). Sem sinal forte mas última msg tem `dtf textil` → TRANSFER (`fresh_dtf_textile_technique`, mjs 138–142). Sem `sistema_config`, sem DB.
- **Tools**: FINISHED → REMOVE `consultar_tabela_dtf, calcular_dtf_por_arte, calcular_dtf_metro` (mjs 6–10); TRANSFER → REMOVE `orcar_camisetas, consultar_modelos` (mjs 158). Em FINISHED, se o modelo ainda usar tool DTF → 409 (mjs 33–35).
- **Leituras/escritas**: nenhuma.
- **Efeito na resposta final**: (a) força `slots.produto` para `camiseta`/`dtf_textil` (mjs 179); (b) contradição em FINISHED (slot dizia dtf_textil): apaga chaves monetárias do JSON (`preco|price|valor|amount|subtotal|orcamento|cotacao`, exceto frete, mjs 12–27, 181–186) e, se `mensagem` tem `R$`, substitui por `'Perfeito. Você quer camiseta básica ou baby look?'` (mjs 192), senão troca "DTF textil" por "camisetas personalizadas" (mjs 194); (c) FINISHED + payload cita dtf/dft/ftp com `R$` ou campo monetário → devolve HTTP **409** `{type:'error', error:{type:'service_mode_commercial_conflict'}}` (126–151) → no núcleo vira `anthropic_indisponivel` (core 3339–3342) e a decisão fica `responde:false` (core 3500/3633) — cliente sem resposta (INFERIDO). Header `x-cortex-service-mode-precedence`.
- **Decisões comerciais**: (modo de serviço) precedência do último sinal forte do cliente; (camiseta) em FINISHED proíbe tabela/preço de DTF; (preço) apaga valores e pergunta "básica ou baby look?"; (DTF) "dtf textil" fresco sem sinal = transfer-only (mjs 138) — decisão forte com evidência fraca; (multi-item) não suporta cliente que quer camiseta + filme (um modo só).
- **"Skill" real?**: n/a.
- **Duplicidade/conflito**: core 2234–2235 (camiseta via orcar_camisetas; estamparia em peça do cliente → Tamires) vs TRANSFER que remove orcar_camisetas/consultar_modelos enquanto core 2268 exige consultar_modelos "para QUALQUER modelo citado" → conflito PROVADO (mjs 158 x core 2268). Core 2187 "SIGA O CLIENTE... Se perguntar de outro produto, responda sobre esse" vs bloqueio 409 (126–151) e reescrita "Perfeito. Você quer camiseta básica ou baby look?" (mjs 192) que descarta a resposta com preço → conflito PROVADO com core 2213/2257. Sobreposição tripla com transfer-only (12–15) e disambiguation (76–95): regexes diferentes para o mesmo conceito. Mensagem fixa "camiseta básica ou baby look" (mjs 192) vs core 2234 "NAO decore lista de modelos" — conflito PROVADO.

### 13. preloads__joao-financial-input-provenance-guard-v1.ts
- **Mecanismo de hook**: `globalThis.fetch` (167) para Anthropic (169); ignora `stream:true` (172). Pré (só curto-circuito ack) + pós (bloqueio de tools / strip de slots). NOTA: `jfpgSanitizeSystem` (26–34) existe mas NUNCA é chamada — a requisição vai intacta (181) → código morto PROVADO; a "Referência: arte de caneca costuma ser 10 x 21cm" do core 2211 NÃO é sanitizada. Depois do núcleo (index.ts:25).
- **Injeção no prompt**: NENHUMA efetiva.
- **Condição de inclusão**: (a) inbound só com linhas de ack (`ta bom|certo|ok|beleza|obrigado|valeu|perfeito|show|boa noite|tchau...`, 66–71) E msg anterior do assistente é encerramento (`fico no aguardo|aguardo comprovante|qualquer coisa é só chamar|pedido esta reservado|pix... continua valendo|bom descanso`, 72–75) → `responde:false`; (b) resposta com `tool_use calcular_rendimento_uv` cuja largura/altura não aparecem nas últimas 30 msgs do cliente (pares `AxB`, "N cm de largura ... M cm de altura", comprimento/altura, 82–98) nem no bloco `[DTF_FILE_PREFLIGHT_CANONICO]` (99–105); ou `quantidade_desejada` sem "N adesivos/unidades/cópias/etiquetas/peças" no texto do cliente (106–112); (c) `calcular_frete` com `cep_destino` que não está em texto do cliente (113–117) nem no system como `cep: 12345-678` + `cep_confirmado_para_envio: true`/`cep_origem: cliente_confirmado|estado_confirmado` (118–124; marcadores existem no core 1010/5008–5009 como slots — PROVADO que existem, INFERIDO que aparecem no system via blocoEstado). Sem `sistema_config`, sem DB.
- **Tools**: intercepta `calcular_rendimento_uv` e `calcular_frete` (bloqueia execução substituindo a resposta, 189–207). Não altera lista.
- **Leituras/escritas**: nenhuma.
- **Efeito na resposta final**: (a) `{responde:false, tema:'despedida', etapa:'despedida'}` → núcleo não responde (177); (b) `'Qual é a medida do adesivo em cm, largura x altura?'` (tema `adesivo_uv`), `'Quantos adesivos você precisa?'`, `'Qual é o CEP de entrega?'` (tema `frete`) (133–136), model 'cortex-financial-input-provenance'; (c) strip de slots: apaga `slots.arte/medidas/largura_cm/altura_cm` e `slots.cep` sem proveniência (137–165). Header `x-cortex-financial-input-provenance`.
- **Decisões comerciais**: (follow-up) ack após encerramento = silêncio; (medidas) só medidas ditas pelo cliente/preflight; (quantidade) só quantidade dita com unidade; (frete/CEP) só CEP dito pelo cliente ou confirmado no estado; (DTF UV) bloqueia cálculo UV sem proveniência (não cobre `calcular_dtf_por_arte` — só UV, 189); (pergunta) reformula como pergunta única.
- **"Skill" real?**: n/a.
- **Duplicidade/conflito**: core 2211 "CLIENTE SEM MEDIDA: pergunte a medida do OBJETO e ofereça opções proporcionais. Referência: arte de caneca costuma ser 10 x 21cm" — o guard quer neutralizar (28–30) mas não aplica; em runtime, se o modelo usar 10x21 do prompt, o guard bloqueia o tool (191–194) → o prompt induz e o guard pune: conflito PROVADO (core 2211 x preload 189–194). Core 2201 "OPÇÃO QUE VOCÊ OFERECEU... ele ESCOLHEU UMA OPÇÃO" — opção oferecida pelo assistente (7x3,1cm) e escolhida como "pode ser essa" não consta em texto do CLIENTE com par numérico → `calcular_rendimento_uv` bloqueado → conflito PROVADO (preload 96–98 só olha textos `user`). Core 2222 "reutilize o CEP que você já tem" vs CEP só do cliente/estado confirmado (118–124). Duplicidade com image-measure (mesmo tool, 76) e com freight-agent preloads (fora do lote). Core 3035–3051 já trata "confirmacao_curta_pos_encerramento" com reação ❤️ — o ack-burst (175–178) duplica e, por ser mais externo, retorna `responde:false` antes (mas o core decide reação antes de chamar o cérebro, core 3035 → INFERIDO que o guard só pega os casos que passaram do core).

### 14. preloads__joao-file-state-orchestrator-v1.ts
- **Mecanismo de hook**: `globalThis.fetch` (234) em DOIS ramos: (A) qualquer `POST` que NÃO seja Anthropic (238–258) → inspeciona saídas para `https://api.z-api.io/instances/*/token/*/send-text` (204), `https://backend.botconversa.com.br/api/v1/webhook/subscriber/<id>/send_message` com `type:'text'` (209), e `*/functions/v1/joao-tts` (214) — corresponde aos envios do core 2412/152/2557 (PROVADO); (B) Anthropic `/v1/messages` (260) pré (curto-circuito) + pós (reescrita). Depois do núcleo (index.ts:26) — é o wrapper MAIS EXTERNO.
- **Injeção no prompt**: NENHUMA.
- **Condição de inclusão**: telefone extraído do system/msgs (`55\d{10,11}`, 34–40, ignorando um número fixo hardcoded em 39). Sessão: `globalThis.__joaoCanonicalActiveTurnV1(phone)` (definido em `joao-canonical-live-e2e-preload-v1.ts:749`, PROVADO) → `canonical_session_state` por session_id/phone → fallback `agente_noturno_estado` (86–137). `product` = `session_state.product.family` ou regex `dtf uv` / `dtf textil` / `impressao dtf ... camisa|tecido` nas últimas 10 msgs do cliente (77–85). Só age se `product`. Estado detectado por regex no inbound (41–67): `SEPARATE_ARTWORKS`, `MOUNTED_FILE`, `NO_ART`, `UNKNOWN`. Bypass se inbound pede preço/tabela (68–75, 277). `dry_run` do turno ativo → `effectZero` (270): não persiste, link fake `token=CONTROLLED_REPLAY` (143, 158). Sem `sistema_config`. Sem URL/service → sessão null; ainda assim responde sinteticamente (session pode ser null e `jfsPersistFileState` retorna null, 142) — fail-open no texto.
- **Tools**: nenhuma alteração de lista; nunca bloqueia tool_use diretamente.
- **Leituras/escritas**: LEITURA `canonical_session_state` (88, 93), `agente_noturno_estado` (106), `lead_identificadores` (195, para mapear subscriber botconversa → telefone). **ESCRITA** RPC `fn_joao_artwork_state_apply_v1` (`FILE_STATE_SET`, com retry em `VERSION_CONFLICT`, 141–156) para MOUNTED_FILE/SEPARATE_ARTWORKS/NO_ART (280, 289, 293); RPC `fn_joao_upload_context_sign_v1` (gera token de upload, 157–162). Host externo: monta link `https://app.skillprintestamparia.com.br/upload-dtf?token=...` (161).
- **Efeito na resposta final**: (a) MOUNTED_FILE → `'Perfeito. Como o arquivo já está montado, não preciso saber quantos adesivos existem dentro dele nem o tamanho de cada arte. Envie o arquivo por este link seguro: <link>'` ou variante "Não consegui gerar o link seguro agora." (283–286), slots `{produto, arquivo_estado}`; (b) SEPARATE_ARTWORKS → `'Perfeito. Você já tem as artes separadas. Você quer que a Skillprint monte o arquivo de impressão para você?'` (290); (c) NO_ART → `'Sem problema. Você pode seguir por três caminhos: usar um pack de artes, montar no nosso Studio ou solicitar criação da arte. Qual caminho você quer usar?'` (294); (d) UNKNOWN + produto explícito no inbound ou respondendo à pergunta de arquivo → `'Antes de falar em quantidade ou tamanho das artes: você já tem o arquivo montado e pronto para impressão, tem as artes/imagens separadas ou ainda não tem a arte?'` (297–300); todos `etapa:'sondagem'`, model 'cortex-file-state-orchestrator'. (e) Pós: se sessão tem `artwork.file_state==='MOUNTED_FILE'` e o modelo pergunta quantidade/medida (`quantas cópias/adesivos`, `qual ... tamanho|medida`, 170–175) → substitui por mensagem canônica: com preflight+pricing ERP: `'Seu arquivo já foi medido pelo preflight: X m de DTF têxtil. O valor canônico do pedido é R$ Y. Quer seguir com envio ou retirada?'` (etapa `orcamento`), ou `'Seu arquivo já está montado e vinculado/em análise. Não preciso da quantidade nem das medidas internas...'` (176–186, 221–232). (f) Ramo outbound: reescreve o campo `message`/`value`/`texto` do POST ao provedor (z-api/botconversa/joao-tts) pela mesma mensagem canônica quando o texto final contém pergunta proibida e a sessão está MOUNTED_FILE (242–255) — reescreve a mensagem JÁ decidida e ledgerizada pelo núcleo (INFERIDO: o ledger `joao_envios` do core guarda a decisão original, não o texto trocado).
- **Decisões comerciais**: (upload/arquivo) classifica estado do arquivo e persiste; (quantidade/medidas) proíbe perguntar quando arquivo montado; (preço) verbaliza `chargeable_total` da sessão canônica com "R$" (180) — autoridade de preço sem tool; (proposta/fechamento) pula para "envio ou retirada?" (etapa `orcamento`); (arte) oferece pack/Studio/criação; (pergunta de preço) bypass quando o cliente pede preço; (DTF) só para famílias dtf_textil/dtf_uv; (upload) gera link assinado.
- **"Skill" real?**: n/a (orquestrador).
- **Duplicidade/conflito**: DUPLICA core 2267 "CLIENTE COM ARQUIVO MONTADO: ... NAO pergunte tamanho de estampa nem quantas copias. Peca o ARQUIVO ou a METRAGEM TOTAL" e core 2266 "DOIS PERFIS DE CLIENTE" (preload 41–67/170–175 x core 2266–2267). Core 2216 "SLOTS: produto -> arte -> quantidade" e core 2265/2269 (classificação de estampa) vs proibição de perguntar quantidade/medida (170–175) — conflito quando MOUNTED_FILE. Core 2209 "[ARQUIVOS REAIS DESTE LEAD] ... Não peça reenvio de arquivo que consta como armazenado" (core 1217) vs preload que manda o link de upload mesmo quando o arquivo já está armazenado (284, não consulta blocoArquivos) → conflito PROVADO. Core 2220–2223 (fechamento: modalidade → CEP → frete) vs "Quer seguir com envio ou retirada?" (180) compatível. Conflito com transfer-only (137 pede "medida da arte e quantas cópias") e material-technique (75–76 idem) e disambiguation (108–109 pede medida) e financial-guard (134 "Quantos adesivos você precisa?") — quatro preloads pedem quantidade/medida que este proíbe; como este é o mais externo, as perguntas deles chegam a ele na volta (pós 304–316) e são trocadas quando a sessão está MOUNTED_FILE; mas os curtos-circuitos sintéticos deles (Response 200 com JSON) também passam pela reescrita 308–315 → efeitos cruzados PROVADOS pela ordem de import.

---

## Tabela-resumo

| arquivo | hook | sempre/condicional | escreve no banco? | altera tools? | bloqueia/reescreve saída? |
|---|---|---|---|---|---|
| joao-image-measure-provenance-guard-v1 | fetch→Anthropic (pós) | condicional: turno com imagem e medida não provada | não | intercepta tool_use (dtf_por_arte/rendimento_uv) → substitui resposta | sim: pergunta de medida fixa; reescreve msg com `NxN` |
| joao-dtf-material-capability-skill-v1 | fetch→Anthropic (pré) + GET ERP tecnicas | condicional: regex material/aplicação + ERP ok | não | não | não |
| joao-price-change-pix-gate-v1 | fetch→Anthropic (pós) + GET operacoes_financeiras | condicional: tool_use gerar_pix + variação >20%/>R$10 ou mudança material | SIM: sistema_logs | intercepta gerar_pix → substitui resposta | sim: "O valor atualizado ficou em R$... Posso gerar o Pix nesse valor?" |
| joao-dtf-textile-transfer-only-skill-v1 | fetch→Anthropic (pré+pós) + GET ERP produtos | condicional: regex transfer-only | não | REMOVE 5 tools; converte tool_use bloqueado em texto | sim: curto-circuito (corte), reescrita cor/modelo/corte |
| joao-apparel-catalog-skill-v1 | fetch→Anthropic (pré+pós) + ERP (2 GET + RPC) | condicional: regex apparel + pergunta/quantidade + produto ERP | não | REESCREVE description de orcar_camisetas | sim: reescreve msg com mínimo 10 |
| joao-artwork-gold-skill-v1 | fetch→Anthropic (pré+pós / curto-circuito) | condicional: regex dourado/metálico | não | não | sim: curto-circuito (físico) e reescrita de claim metálico |
| joao-dtf-material-technique-gate-v1 | fetch→Anthropic (pré+pós) | condicional: regex têxtil xor rígido | não | REMOVE tools UV ou têxteis; converte tool_use errado | sim: reescreve msg que cita técnica oposta |
| joao-bag-material-clarification-gate-v1 | fetch→Anthropic (curto-circuito) | condicional: sacola/bolsa sem material | não | não | sim: sempre substitui por pergunta de material |
| joao-sheet-choice-continuity-gate-v1 | fetch→Anthropic (pré+pós) + ERP RPC quote | condicional: inbound "A3"/"A4" após pergunta A3/A4 | SIM: sistema_logs (quando corrige) | não | sim: remove repergunta, corrige formato e preço unitário pelo ERP |
| product-service-disambiguation-preload-v3 (+context-core) | fetch→Anthropic (curto-circuito) + GET sistema_config | condicional: flag `joao_qualification_gate_ativo`=true (fail-closed) + regexes | SIM: sistema_logs | não | sim: substitui por clarificação / resposta "só o adesivo/DTF" |
| joao-dtf-objection-continuity-skill-v1 (+core.mjs) | fetch→Anthropic (curto-circuito) | condicional: dtf textil+metros no histórico + objeção | não | não | sim: texto fixo "Desculpa, me expressei mal... 1 metro..." |
| joao-service-mode-precedence-preload-v1 (+core.mjs) | fetch→Anthropic (pré+pós) | condicional: sinal forte de modo ou "dtf textil" fresco | não | REMOVE tools DTF (FINISHED) ou orcar/consultar_modelos (TRANSFER) | sim: força slots.produto, apaga R$, msg fixa; HTTP 409 em resíduo comercial |
| joao-financial-input-provenance-guard-v1 | fetch→Anthropic (pré ack-burst + pós) | condicional: ack pós-encerramento; tool_use rendimento_uv/frete sem proveniência | não | intercepta calcular_rendimento_uv e calcular_frete → substitui resposta | sim: `responde:false`; perguntas fixas de medida/quantidade/CEP; strip de slots |
| joao-file-state-orchestrator-v1 | fetch→Anthropic (pré/pós) + fetch→z-api/botconversa/joao-tts (POST saída) + REST/RPC Supabase | condicional: produto DTF identificado + estado de arquivo | SIM: RPC fn_joao_artwork_state_apply_v1, fn_joao_upload_context_sign_v1 | não | sim: 4 curtos-circuitos fixos; reescrita pós; reescrita do texto enviado ao provedor |

## Observações transversais (para o laudo)
- Nenhum arquivo do lote implementa "skill" no sentido de descoberta/manifesto/progressive disclosure: os 5 nomeados "skill" (2, 4, 5, 6, 11) são regex de ativação + bloco de texto e/ou resposta fixa; três deles (2, 4, 5) buscam dados no ERP em runtime.
- Detectores redundantes de "modo de serviço" em 4 arquivos (4, 10, 11, 12) com regexes divergentes; remoção de tools em 3 (4, 7, 12) e interceptação do mesmo tool `calcular_rendimento_uv` em 2 (1, 13).
- Autoridade de preço declarada "ausente" mas exercida em texto: 3 (valores em R$ do banco), 9 (unit_price ERP sobrescrevendo o texto), 14 (`chargeable_total` da sessão).
- Código morto PROVADO: 6 (`jagsRule(true)` nunca injetado), 13 (`jfpgSanitizeSystem` nunca chamada). Bug PROVADO: 11 ("1 metro" hardcoded, linha 86, ignora `quantity`).
- Único fail-closed por flag: 10 (`joao_qualification_gate_ativo`); todos os demais são sempre-ativos por regex, sem chave em `sistema_config`.
- Único que grava estado de negócio: 14 (RPCs de estado de arquivo e token de upload); 3, 9 e 10 gravam só `sistema_logs`.